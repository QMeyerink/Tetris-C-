#ifndef COMMUNICATION_H
#define COMMUNICATION_H

/** Check if a char has been recieved by player. */
int recieve_char(char recieve);

/** Send a specified char to other player */
void send_char(char send);


#endif
